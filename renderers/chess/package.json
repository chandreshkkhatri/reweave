{
  "name": "reweave-chess-renderer",
  "version": "0.1.0",
  "private": true,
  "description": "Chessground + Playwright chess board renderer (proof-of-concept)",
  "scripts": {
    "postinstall": "npx playwright install chromium",
    "render": "node render.js"
  },
  "dependencies": {
    "@lichess-org/chessground": "^9.0.0",
    "playwright": "^1.50.0"
  }
}
